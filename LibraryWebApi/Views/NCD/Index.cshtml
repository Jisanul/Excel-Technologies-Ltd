@model IEnumerable<LibraryManagementSystem.Models.NCD>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NCDID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NCDName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NCDID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NCDName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.NCDID }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.NCDID })
            </td>
        </tr>
}
    </tbody>
</table>
